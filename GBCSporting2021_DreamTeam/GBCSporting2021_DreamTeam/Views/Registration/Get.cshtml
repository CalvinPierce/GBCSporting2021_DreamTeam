@model RegistrationViewModel
@{
    ViewBag.Title = "Get Customer";
}

<h2>Get Customer</h2>
<form asp-action="Set" method="post" onsubmit="return getCustomerValidation()">

    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="form-group">
        <select class="form-control" name="id" id="getCustomerSelect">
            <option value=""></option>
            @foreach (Customer c in ViewBag.Customers)
            {
                <option value="@c.CustomerId">@c.FullName</option>
            }
        </select>
    </div>

    <button type="submit" class="btn btn-primary">Select</button>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Cancel</a>
</form>
